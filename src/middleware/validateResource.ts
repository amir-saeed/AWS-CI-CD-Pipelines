import { Request, Response, NextFunction } from 'express';

const validate = (shema) => (req: Request, res: Response, next: NextFunction) => {
    try {

    }
    catch (error: any) {

    }

}

export default validate;